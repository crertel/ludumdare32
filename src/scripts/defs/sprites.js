(function(self){
    "use strict";
    self.CD = self.CD || {};
    var CD = self.CD;
    CD.sprites = (function(){
        return [
        ];
    })();
})( (typeof window === "undefined")?self:window);
